{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "qa.v1 output",
  "type": "object",
  "required": ["status", "text"],
  "additionalProperties": false,
  "properties": {
    "status": { "enum": ["answered", "notInDocuments", "needsLawyer"] },
    "text": { "type": "string" },
    "citations": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["spanId", "quote"],
        "additionalProperties": false,
        "properties": {
          "spanId": { "type": "string", "pattern": "^D\\d+-P\\d+-S\\d+$" },
          "quote": { "type": "string" }
        }
      }
    }
  }
}
