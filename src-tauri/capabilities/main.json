{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Least-privilege capability set for the single Olai window. Only the file-open dialog and the app's own IPC commands are permitted. No shell, no filesystem scope, no HTTP plugin.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:event:default",
    {
      "identifier": "dialog:allow-open",
      "allow": [
        {
          "name": "Choose legal documents",
          "extensions": ["pdf", "png", "jpg", "jpeg"]
        }
      ]
    }
  ]
}
