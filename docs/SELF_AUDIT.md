# Self‑Audit

Scored against the six evaluation criteria, with concrete evidence (files, commands,
results). Results below are from actual runs on Windows 11 with the Rust MSVC toolchain.

**One‑command gate:** `npm run verify` → **PASS** (lint · typecheck · format · frontend
coverage · Rust tests · npm+cargo audits · build).

| Criterion | Score | Evidence | Honest gaps |
| --- | --- | --- | --- |
| **Code Quality** | Strong | Layered backend `commands → services → providers` (`LlmProvider` trait + `MockProvider`/`AnthropicProvider`, DI). Typed errors (`thiserror`, `error.rs`), no `unwrap/expect` outside tests, `#![forbid(unsafe_code)]`, `#![warn(missing_docs)]`. `cargo clippy --all-targets --all-features -D warnings` → **exit 0**; `cargo fmt --check` → **exit 0**. Frontend: TS strict, ESLint strict + jsx‑a11y, Prettier — all clean. Shared types generated by `ts-rs` (`src/lib/bindings/`), no `any`, `dangerouslySetInnerHTML` banned by lint. | A few files approach the ~300‑line guideline; screenshots not yet captured. |
| **Security** | Strong | Key only in Windows Credential Manager (`secrets.rs`); all network in Rust (`reqwest`+`rustls`). Strict CSP + least‑privilege capabilities (`tauri.conf.json`, `capabilities/main.json`). Magic‑byte validation + size/page/count caps (`extraction.rs`). PII masking (`pii.rs`), injection/hidden‑text scan (`injection.rs`), verdict filter (`safety.rs`). **STRIDE** table + data flow in [SECURITY.md](../SECURITY.md). `npm audit --omit=dev` → **0**; `cargo audit` → **0 error‑level** (removed dead `validator` → cleared idna RUSTSEC‑2024‑0421). | Dev‑only JS advisories tracked, not shipped (DECISIONS D9); `cargo audit` has 7 allowed unmaintained/unsound warnings (Linux‑only `glib`, `unic-*`). |
| **Efficiency** | Strong | **NSIS installer 2.22 MB / MSI 3.31 MB** (≪ 15 MB target); release binary 5.90 MB; frontend 293 KB raw / **87 KB gzip**. PDF parsing in a Web Worker; async `tokio` services with concurrent model calls; SHA‑256 doc cache; model tiering; Criterion benches (`benches/`). Small release profile (LTO, `opt-level="s"`, strip, `panic="abort"`). See [PERFORMANCE.md](../PERFORMANCE.md). | Idle **private** memory and cold‑start not captured on a clean machine (method documented; Rust host ~23 MB). |
| **Testing** | Strong | **Rust:** 113 unit + 8 integration pass (`pipeline_demo.rs` asserts conflict count, forced tier, no unverified‑as‑fact, grounded Q&A, strategy refusal, schema repair). **Frontend:** 64 tests, coverage **98.18% lines / 89.96% branches** (thresholds enforced). **E2E:** 3 Playwright specs pass (demo, keyboard‑only, injection). Audits + build in CI on windows‑latest. See [TESTING.md](../TESTING.md). | Rust `cargo llvm-cov` threshold wired in CI but not re‑measured here; e2e uses mocked IPC (by design). |
| **Accessibility** | Strong | `vitest-axe` **0 violations** on Onboarding, Dashboard (all tabs + compare + citation drawer), scam variant, and components. Skip link, `?` shortcuts dialog, `J`/`K` + arrow‑key nav (e2e keyboard spec), focus‑visible, `aria-live`, forced‑colors + reduced‑motion, severity by icon+word, 44px targets, per‑language `lang`, bundled Noto fonts, TTS. See [ACCESSIBILITY.md](../ACCESSIBILITY.md). | Windows **Narrator** manual pass is a documented checklist, not yet executed/dated. |
| **Problem Statement Alignment** | Strong | **Understand:** Paper Card + grounded claims (cite‑or‑hide, ≥0.9/≤25 words) + in‑code deadlines with shown math. **Compare:** synced panes, numbered conflicts, demo shows 7‑day vs one‑month (clause 9). **Navigate:** cited Protect/Gather/Respond/GetHelp checklist, `.ics`, grounded Ask, neutral reply, Get‑help card. EN/TA/HI + TTS. Offline **demo mode** (eviction + scam). Persistent disclaimer. See [RESPONSIBLE_AI.md](../RESPONSIBLE_AI.md). | Legal‑aid pack ships as **TODO placeholders** (by design; `verifiedOn=null` hidden in release) — the team must fill real, dated sources before public release. |

## Verified command log (this session)

- `cargo test --no-default-features` → `108 passed; 0 failed` (+ 8 integration).
- `cargo test` (app feature) → `113 passed; 0 failed` (+ 8 integration).
- `cargo clippy --all-targets --all-features -- -D warnings` → exit 0.
- `cargo fmt --all --check` → exit 0.
- `npm run test:coverage` → `64 passed`, lines 98.18%.
- `npm run test:e2e` → `3 passed`.
- `npm audit --omit=dev --audit-level=high` → `found 0 vulnerabilities`.
- `cargo audit` → `0` error‑level (7 allowed warnings).
- `npm run verify` → PASS (ends with a successful `vite build`).

## Follow‑ups before public release
1. Fill the legal‑aid knowledge pack with verified, dated sources.
2. Run and date the Windows Narrator manual checklist (ACCESSIBILITY.md §7).
3. Capture private‑working‑set memory and cold‑start on clean target hardware.
4. Add real screenshots to `docs/screenshots/`.
5. Plan the vite/vitest toolchain upgrade to clear the dev‑only advisories (DECISIONS D9).
